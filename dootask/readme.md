
```bash
# 使用gitee克隆项目到您的本地或服务器
git clone https://gitee.com/aipaw/dootask.git

# 进入目录
cd dootask

# 一键安装项目（自定义端口安装 ./cmd install --port 2222）
./cmd install

# 一键启动
docker-compose up -d

# 一键删除
docker-compose down
```
